CREATE TABLE Kinobilletter(
        id INTEGER AUTO_INCREMENT NOT NULL,
        valgtfilm VARCHAR(250) NOT NULL,
        antall VARCHAR(250) NOT NULL,
        fnavn VARCHAR(250) NOT NULL,
        enavn VARCHAR(250) NOT NULL,
        tel   VARCHAR(250) NOT NULL,
        epost  VARCHAR(250) NOT NULL,
        PRIMARY KEY (id)
);



